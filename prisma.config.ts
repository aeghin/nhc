
import "dotenv/config";

// Prisma 7+ doesn't require a separate config file
// Configuration is handled through the prisma/schema.prisma file
// Ensure DATABASE_URL is set in your .env.local or environment variables
