"use client";

// Single import path for the UI. Swap to a Pusher/PartyKit hook here later;
// the component contract (UseEventChatReturn) stays identical.
export { useEventChat } from "./ably-use-event-chat";
